/* eslint-disable import/prefer-default-export */
import styled from "styled-components";

const StyledSuggestionBox = styled.div`
  width: 300px;
  height: auto;
  padding: 15px;
  background: #fff;
`;

const StyledSuggestionText = styled.span`
  font-size: 24pt;
  color: #484848;
`;
const StyledSuggestionSubtext = styled.span`
  font-size: 14pt;
  color: #767676;
`;

export { StyledSuggestionBox, StyledSuggestionText, StyledSuggestionSubtext };
